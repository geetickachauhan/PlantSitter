//listeners for profile.js buttons and inputs
//dynamically loads the content of profile
function showProfile(own){
    // own is a flag that if set to 1, displays calendar, edit and add event to calendar
    
}